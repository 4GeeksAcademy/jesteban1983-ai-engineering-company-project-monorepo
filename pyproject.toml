[project]
name = "trackflow-sales-forecasting"
version = "0.1.0"
description = "Chronological sales forecasting pipeline for TrackFlow"
requires-python = ">=3.12"
dependencies = [
  "matplotlib==3.10.0",
  "pandas==2.2.3",
  "scikit-learn==1.6.1",
  "scipy==1.15.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
