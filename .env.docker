# ============================================
# .env.docker - Variables de entorno para Docker
# ============================================
# Copia este archivo a .env si quieres usar un archivo local
# en lugar de exportar variables manualmente.
#
# Uso: docker compose --env-file .env.docker up -d
# ============================================

# Contraseña de la base de datos PostgreSQL
DB_PASSWORD=devpassword