# ============================================
# Dependencias del backend de inventario
# ============================================
# FastAPI - Framework web para crear APIs REST
fastapi[standard]>=0.115.0

# SQLAlchemy - ORM (Object-Relational Mapper) para trabajar con bases de datos
sqlalchemy>=2.0.0

# Alembic - Herramienta de migraciones para bases de datos
alembic>=1.13.0

# asyncpg - Driver asíncrono para PostgreSQL (producción)
asyncpg>=0.30.0

# aiosqlite - Driver asíncrono para SQLite (desarrollo)
aiosqlite>=0.20.0

# Pydantic - Validación de datos (ya viene con FastAPI pero explícito)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Uvicorn - Servidor ASGI para correr FastAPI
uvicorn[standard]>=0.35.0

# python-dotenv - Cargar variables de entorno desde .env
python-dotenv>=1.0.0

# --- Dependencias de testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
pytest-cov>=5.0.0